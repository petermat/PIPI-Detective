{
  "queries": {
	"python_process_events": {
  	"query": "SELECT * FROM bpf_process_events;",
  	"interval": 10,
  	"description": "Monitor all syscalls.",
  	"removed": false
	},
	"python_socket_events": {
  	"query": "SELECT * FROM bpf_socket_events;",
  	"interval": 10,
  	"description": "Monitor all socket events.",
  	"removed": false
	}
  },
  "version": "1.0.0",
  "description": "Pack to monitor all syscalls and socket events.",
  "shards": 100
}
